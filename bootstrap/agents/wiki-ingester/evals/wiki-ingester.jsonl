{"id": "plain-article-staged", "input": "Ingest https://www.anthropic.com/engineering/some-article into agentic-design, staged", "expected": "One staged entry in _inbox/proposed/ with all required frontmatter fields + status: proposed; a dot-form <slug>.proposed_metadata.json sidecar with a FULL-PATH target_folder (research/<sub>) and <=8 typed suggested_backlinks objects; raw artifact in raw/; recorded eval scores avg >= 3.0; receipt row with slug/folder/tier/conf/eval; nothing written under wiki/."}
{"id": "batch-sequential-reconciles", "input": "Ingest these 4 URLs into agentic-design, staged: <url1> <url2> <url3> <url4>", "expected": "Four items processed strictly sequentially (no parallel fetches); receipt has exactly 4 rows; reconciliation line balances (4 assigned = staged + failed + skipped); receipt total <= ~2K tokens regardless of source sizes."}
{"id": "dead-url-loud-failure", "input": "Ingest https://deadblog.example/gone-post into agentic-design, staged (URL 404s on urllib and on the fallback fetcher)", "expected": "Item marked FAILED in the receipt with the concrete reason (404 on both fetchers), after exactly one fallback attempt — no blind retries, no silent drop, no fabricated entry, batch continues to any remaining items."}
{"id": "github-repo-depth", "input": "Ingest https://github.com/example/agent-toolkit (a repo with 40 skills and 8 agents beyond its README) into agentic-design, staged", "expected": "Read goes BEYOND the README-rewrite default: tree structure + docs/ + key files inspected; the entry inventories the repo structurally (component counts stated and blockquote-sourced, e.g. '40 skills / 8 agents'), not just a README summary."}
{"id": "youtube-asr-integrity", "input": "Ingest a YouTube talk about Claude Code whose auto-captions render 'Claude Code' as 'Cloud Code' into agentic-design, staged", "expected": "Full transcript read; mishearings of key terms corrected before any blockquote; ONE dated transcription note near the top of the entry (not per-instance annotations); any passage that can't be confidently disambiguated is paraphrased (sourced), not blockquoted."}
{"id": "queue-drain-resumable", "input": "Drain the pending queue for agentic-design (6 .queue files), staged; the run is interrupted after item 3", "expected": "Items 1-3: entries staged AND their .queue files moved pending -> done as EACH completed (not batched at the end); items 4-6 remain untouched in _inbox/pending/ so a fresh worker resumes exactly there from disk state; no half-processed item."}
{"id": "eval-gate-fail-no-stage", "input": "Ingest a thin listicle that scores eval avg 2.4 after one fix-and-rescore round, staged", "expected": "Entry is NOT staged to _inbox/proposed/; item reported failed-eval in the receipt; a review note written to _inbox/temp/<slug>.eval-failed.md with per-dimension scores + content-grounded rationale (quoted passages) + what would fix it; draft synthesis preserved alongside; worker does not stall waiting for a human."}
