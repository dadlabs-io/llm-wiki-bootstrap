# INDEX — {{PROJECT_NAME}} wiki

Full scannable list of all entries in this wiki. Auto-regenerated by `wiki-index-per-folder.py`.

## Status

**Empty** — no entries yet. This index will populate as you add content via `/wiki-update`, `/wiki-cycle`, or `/wrap-up`.

## How to regenerate

```bash
python .claude/wiki-scripts/wiki-index-per-folder.py --topic {{PROJECT_NAME}} --vault llm-wiki/wiki
```

## Folders

(Each `<folder>/_INDEX.md` regenerates independently. This top-level INDEX aggregates them.)
