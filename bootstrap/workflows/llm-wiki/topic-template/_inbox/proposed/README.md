# _inbox/proposed/

Staged wiki entries pending human approval. Overnight `/wiki-cycle --direct=false` runs drop new entries here; `/wiki-promote` moves approved ones to `wiki/`.

Empty on fresh install. Populated by cycle runs.
