---
title: "upd-docs — skill"
type: how-to
artifact: skill
name: upd-docs
installed_by: install-wiki
date: 2026-07-31
---

# upd-docs — skill

**Retired.** Its job — refreshing the working-memory dashboards under `sessions/` (`handoff.md`, `task.md`, and `active-context.md`) — is now done by [`wrap-up`](./wrap-up.md) on every run, so session-close is a single command and the resume pointer is never left stale.

**Trigger:** typing */upd-docs* (or "update docs", "save progress", "save state") redirects to `/wrap-up`. The stub exists only so old muscle memory lands somewhere sensible.
