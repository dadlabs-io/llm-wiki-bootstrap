# {{PROJECT_NAME}}

{{PROJECT_DESCRIPTION}}

## Status

In active development.

## Wiki

A unified wiki lives at `{{WIKI_PATH}}/` — ingested research and your own project knowledge side by side (four-layer memory model). The folders it was given at scaffold time:

{{WIKI_FOLDERS}}

For an orientation, read `{{LLM_WIKI_PATH}}/README.md`; for how the whole system works, `{{LLM_WIKI_PATH}}/how-to/llm-wiki/user-guide.md`.

## Working with this project

- Ingest external references (articles, docs, papers): `/wiki-update <url>` for one source now, or `/wiki-cycle` for the research cycle (gather → triage → ingest → check → lint → report)
- Durable project decisions/components are filed inline as you work, and `/wrap-up` catches the rest at session end
- Promote staged entries: `/wiki-promote --review`
- Search prior research + decisions/components: `/wiki-search "<query>"`

See `CLAUDE.md` for the full session rhythm and conventions.

## License

(set this)
