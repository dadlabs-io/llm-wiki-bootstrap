# skilltest — the seed notebook for the wiki-search test baseline

A throwaway notebook the test runner copies into a sandbox for every run. It describes Lantern, a made-up internal search service, so a model cannot answer the test questions from what it already knows: only the entries hold the answers. One entry is a snippet trap (its opening section is a rejected proposal; the decision comes later). Never register this folder as a real notebook.
