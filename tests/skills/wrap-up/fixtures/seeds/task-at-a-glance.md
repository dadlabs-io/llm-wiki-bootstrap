## At a glance

<!-- wiki-tasks next-id: 5 -->
Every task, one line each, by owner. A number is never reused. "waiting" is a status: the task stays with its owner. A task leaves this list only when the user says so; a finished one is marked `done` and the session asks before removing it. Notes on a task go below, keyed by its number.

### Dana

| # | Task | Status | Next / waiting on |
|---|---|---|---|
| 3 | Confirm the object store's etag semantics with the vendor | waiting | vendor reply |

### main

| # | Task | Status | Next / waiting on |
|---|---|---|---|
| 1 | Decide the retry cap: attempt count or elapsed time | doing | pick one and write it down |
| 2 | Chase the duplicate-upload report from the resumed sync | to do | reproduce on a resumed sync |
| 4 | Document the network seam for new contributors | to do | after the retry work |

## NOW
Wiring the retry budget into beacon's upload loop.

## QUEUE
1. Longer notes on task 1 live here.
